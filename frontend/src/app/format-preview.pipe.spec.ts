import { FormatPreviewPipe } from './format-preview.pipe';

describe('FormatPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
